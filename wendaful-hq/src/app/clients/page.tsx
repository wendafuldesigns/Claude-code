import { Briefcase } from "lucide-react";
import { ComingSoon } from "@/components/ui/ComingSoon";

export default function ClientWorkPage() {
  return (
    <ComingSoon
      title="Client Work"
      blurb="SCMC hours and tasks, all in one spot."
      icon={Briefcase}
    />
  );
}
